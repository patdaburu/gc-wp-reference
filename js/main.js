/**
 * This is a very simple javascript file referenced in the shortcode.php file.
 */

// Reload the page every five (5) seconds.
setTimeout(function(){location.reload()}, 5000);

/*
// Refresh the iframe.
setInterval(function(){
    document.getElementById('myIFrame').contentWindow.location.reload();
}, 5000);
*/

